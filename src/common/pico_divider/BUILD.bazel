package(default_visibility = ["//visibility:public"])

cc_library(
    name = "pico_divider",
    includes = ["include"],
    hdrs = ["include/pico/divider.h"],
    deps = [
        "//src/common/pico_base:pico_base",
    ] + select({
        "//bazel/constraint:rp2": ["//src/rp2_common/hardware_divider:hardware_divider"],
        "//bazel/constraint:host": ["//src/host/hardware_divider:hardware_divider"],
    }),
)
