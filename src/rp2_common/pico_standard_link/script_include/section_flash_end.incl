/* Defines the following symbols for use by code:
    __flash_binary_end
*/

SECTIONS
{
    .flash_end : {
        KEEP(*(.embedded_end_block*))
        PROVIDE(__flash_binary_end = .);
    } > TEXT_STORE =0xaa
}
