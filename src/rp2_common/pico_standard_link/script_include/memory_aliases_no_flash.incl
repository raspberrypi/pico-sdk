/* Store everything where it came from (ram/scratch) */

REGION_ALIAS("TEXT_STORE", RAM);
REGION_ALIAS("RAM_STORE", RAM);
REGION_ALIAS("PSRAM_STORE", RAM); /* Can't store in PSRAM */
REGION_ALIAS("XIP_RAM_STORE", XIP_RAM);
REGION_ALIAS("SCRATCH_X_STORE", SCRATCH_X);
REGION_ALIAS("SCRATCH_Y_STORE", SCRATCH_Y);
