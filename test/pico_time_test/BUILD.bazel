load("//bazel:defs.bzl", "compatible_with_rp2")
load("//bazel/util:define_transition.bzl", "binary_with_overridden_defines")

package(default_visibility = ["//visibility:public"])

cc_binary(
    name = "pico_time_test_actual",
    testonly = True,
    srcs = ["pico_time_test.c"],
    tags = ["manual"],  # Built via pico_time_test.
    # Doesn't appear to work on host builds yet.
    target_compatible_with = compatible_with_rp2(),
    deps = [
        "//src/common/pico_stdlib",
        "//test/pico_test",
    ],
)

binary_with_overridden_defines(
    name = "pico_time_test",
    testonly = True,
    binary = ":pico_time_test_actual",
    defines = ["PICO_TIME_DEFAULT_ALARM_POOL_MAX_TIMERS=250"],
    # Doesn't appear to work on host builds yet.
    target_compatible_with = compatible_with_rp2(),
)
